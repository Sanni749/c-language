// w a p to print area of square(a=side*side) user throught
#include <stdio.h>
int main()
{
    int side, area;
    printf("enter the sider=");
    scanf("%d", &side);
    area = side * side;
    printf(" area of square:%d", area);
    return 0;
}